apiVersion: v1
namespace: production
kind: Pod
metadata:
  name: simple-webapp-red
  labels:
    app: web
spec:
  containers:
  - name: web-red
    image: mmumshad/simple-webapp-color
    ports:
      - containerPort: 8080
    env:
      - name: APP_COLOR
        value: red